apply plugin: 'com.android.library'

android {
    compileSdkVersion 36

    defaultConfig {
        minSdk 21
        targetSdk 35
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
}

dependencies {
    // Adicione dependências do backend aqui (SDK Tuya, retrofit, etc.)
}
